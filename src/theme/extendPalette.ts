const white = '#fff';
const lightGray = '#F8F8F8';

const backgroundColorMain = lightGray;

export interface IExtendPalette {
  white: string;
  backgroundColorMain: string;
}

export const extendPalette: IExtendPalette = {
  white,
  backgroundColorMain: backgroundColorMain,
};
